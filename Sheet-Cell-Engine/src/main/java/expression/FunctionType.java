package expression;

public enum FunctionType {
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    MOD,
    CONCAT,
    SUB,
    REF,
    SUM,
    AVERAGE,
    PERCENT,
    EQUAL,
    NOT,
    OR,
    AND,
    BIGGER,
    LESS,
    IF;
}
